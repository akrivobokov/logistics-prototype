
import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import LogisticsPrototype from './LogisticsPrototype';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <LogisticsPrototype />
  </React.StrictMode>
);
